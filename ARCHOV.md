# ARCHOV — חוקי המודולריות של VibeBar

> מה שקיים — לא להחליף. מודולריות = לעטוף בשקע, לא לשכתב.

מסמך זה הוא הוראת־בעלים. במקרה של סתירה, הוא גובר על הצעה ארכיטקטונית
אחרת בענף זה.

## 1. מבנה שקע

לכל גבול חיצוני יש שלושה חלקים:

1. `Protocol` — החוזה שהמערכת רשאית להניח.
2. `Legacy` — מתאם אל המימוש המקורי, בלי להעתיק או לשכתב אותו.
3. `Null` — לוחית אטומה שמיישמת את החוזה בכיוון בטוח.

כל בחירת המימושים נעשית רק ב־`vibebar_modular/sockets.py`. ברירת המחדל
היא `identity`: הסקריפטים המקוריים מקבלים אותן פקודות שקיבלו לפני העטיפה.

## 2. הכיוון הבטוח

שכחה בחיווט אינה רשאית למחוק, לפרסם, להדביק או לדרוס נתונים. פעולות
הרסניות עוברות דרך `RecycleBinSocket`; ברירת המחדל שלו היא
`NullRecycleBin`, שאינו מוחק דבר.

## 3. הרכבות

שקע עונה איזה מימוש מחובר. הרכבה עונה אילו יכולות קיימות במוצר.
`compositions.py` הוא מקור האמת היחיד להרכבות `full` ו־`pilot`.
ממשק רשאי להציג רק פעולות שנמצאות ב־`Composition.actions`.

## 4. מה אינו שקע

- פורמט היומן וחישוב הזמן הם ליבת העסק. אין להחליפם דרך Socket.
- הסקריפטים המקוריים ב־`bin/` נשארים מקור האמת; מתאמי Legacy מפעילים אותם.
- תמלול הוא תהליך עסקי עתידי שמשתמש בשקעי קול, לא Socket יחיד שמסתיר הכול.

## 5. שכבת בסיס וכללי ברזל

`vibebar_modular` היא שכבת העטיפה היחידה. בקוד חדש שבתוכה:

- אין `except Exception` עירום.
- אין `datetime.now()` מחוץ ל־`clock.py`.
- אין retry ידני.
- אין `print()`.
- קובץ עד 400 שורות; פונקציה עד 50 שורות.
- טיפוסים בכל חתימות הממשק.
- אין נתונים אישיים בקוד, בדוגמאות או בבדיקות.

האכיפה סורקת רק קוד שבבעלות הפורק, אך עושה זאת רקורסיבית — כולל `blocks`,
כלי־עזר ובדיקות. סביבות וירטואליות ותלויות צד שלישי מוחרגות במפורש.

## 6. אמת בבדיקות ובתיעוד

- בדיקת CI נקראת על שם מה שהיא באמת מריצה; אין תג ירוק שמרמז על פלטפורמה
  שלא נבדקה.
- בדיקה אוטומטית אינה תלויה במודל, קובץ או נתיב שקיימים רק במחשב המפתח.
- `Null` של פעולה שהמשתמש ביקש מחזיר כישלון מפורש. הוא לעולם אינו מדווח
  הצלחה על פעולה שלא בוצעה.
- הערות מסבירות החלטות ואילוצים לא־ברורים. אין הערות שחוזרות על שורת הקוד.
- אין קובצי re-export שנועדו רק ליצור מראית עין של שכבה. נקודת תאימות נשמרת
  רק כאשר יש לה צרכן חיצוני מתועד.
- סטטוס חומרה נכתב במפורש: בדיקת מתאם אינה תחליף לבדיקת קבלה על התקן אמיתי.

הקוד המקורי נשאר ללא שינוי. כאשר נוגעים בו בעתיד, מחילים את הכללים על
השינוי החדש בלי לשכתב את המימוש רק לשם אחידות.
